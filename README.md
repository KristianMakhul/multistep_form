# Multistep_form

### Installation

1.Clone the repository:

`git clone https://github.com/KristianMakhul/multistep_form.git`

2.Navigate into the project directory:

`cd multistep_form/`

3.Install dependencies:

`npm install`

4.Start the development server:

`npm run dev`
